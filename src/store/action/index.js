import database from '@react-native-firebase/database';

// const get_data = (e,p) => {
//     return (dispatch) => {
//   database().ref('/').child('users').on('child_added', data =>{
//       let allusers = data.val()
//       if(e === allusers.email){
//         if(p === allusers.password){
//             this.props.navigation.navigate('DonorList')
//         }
//     }
//       dispatch({ type: "GETDATA", payload: allusers})
//   })      
//     }
// }

export {
    // get_data
}